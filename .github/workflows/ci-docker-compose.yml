name: Docker Swarm CI

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main

###########################
#      CONFIGURATION      #
###########################
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: ⚙️ Configurer Docker Swarm
        run: docker swarm init || echo "Swarm déjà initialisé ou erreur ignorée"
      
      - name: ⚙️ Vérifier si Docker Swarm est bien actif
        run: docker info | grep -i swarm

      - name: 💾 Sauvegarder le workspace
        uses: actions/upload-artifact@v4
        with:
          name: workspace
          path: ./
          retention-days: 1
          
  matrix_setup : 
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [elk, opencti, openbas]
    steps:
      - name: 🔄 Restaurer le workspace et `.env`
        uses: actions/download-artifact@v4
        with:
          name: workspace
          path: ./
  
      - name: 🔎 Vérifier l'existence des fichiers Docker Compose
        run: |
          for service in elk; do
            if [[ ! -f "$service/docker-compose.yml" ]]; then
              echo "❌ Fichier manquant : $service/docker-compose.yml"
              exit 1
            fi
          done
        
      - name: 🕵️ Vérification de l'existence du fichier .env
        run: |
          ls -la elk/
          cat elk/.env || echo "❌ Fichier .env introuvable !"
      
      - name: 💾 Sauvegarder `.env`
        uses: actions/upload-artifact@v4
        with:
          name: env_file_${{ matrix.service }}
          path: ${{ matrix.service }}/.env

###########################
#      DÉPLOIEMENT        #
###########################
  deploy:
    needs: 
      - setup
      - matrix_setup
    runs-on: ubuntu-latest
    services:
      docker:
        image: docker:dind
        options: --privileged
    strategy:
      matrix:
        service: [elk, opencti, openbas]
    steps:
      - name: 🔄 Restaurer le workspace et `.env`
        uses: actions/download-artifact@v4
        with:
          name: workspace
          path: ./
      
      - name: 🚀 Déployer ${{ matrix.service }}
        run: |
          export $(grep -v '^#' ${{ matrix.service }}/.env | xargs)
          docker stack deploy -c ${{ matrix.service }}/docker-compose.yml ${{ matrix.service }} 2>&1 | tee logs_${{ matrix.service }}.txt

      - name: 📦 Stocker les logs de ${{ matrix.service }}
        uses: actions/upload-artifact@v4
        with:
          name: logs_${{ matrix.service }}
          path: logs_${{ matrix.service }}.txt

###########################
#  DÉTECTION DES ERREURS  #
###########################
  check_errors:
    needs: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [elk, opencti, openbas]
    steps:
      - name: 📥 Récupérer les logs de ${{ matrix.service }}
        uses: actions/download-artifact@v4
        with:
          name: logs_${{ matrix.service }}
          path: .

      - name: 🔍 Vérifier les erreurs dans logs_${{ matrix.service }}.txt
        id: detect_failure
        run: |
          if grep -qi "error\|failed\|cannot\|swarm manager" logs_${{ matrix.service }}.txt; then
            echo "⚠️ Erreur détectée dans logs_${{ matrix.service }}.txt"
            echo "test_failed=true" >> $GITHUB_ENV
          else
            echo "test_failed=false" >> $GITHUB_ENV

###########################
#   GESTION DES LOGS      #
###########################
  push_logs:
    needs: check_errors
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Restaurer le workspace
        uses: actions/download-artifact@v4
        with:
          name: workspace
          path: .

      - name: 🚀 Vérifier `.git/`
        run: |
          if [ ! -d ".git" ]; then
            echo "⚠️ Le dossier .git est manquant, reclonage en cours..."
            git clone https://github.com/${{ github.repository }}.git .
          fi

      - name: 📥 Télécharger tous les logs
        uses: actions/download-artifact@v4
        with:
          path: logs/

      - name: 📝 Générer un fichier de logs consolidé
        run: |
          echo "📅 Date : $(date)" > error-log.txt
          for service in elk opencti openbas; do
            log_file="logs/logs_${service}/logs_${service}.txt"
            if [[ -f "$log_file" ]]; then
              echo "❌ ERREUR POUR $service :" >> error-log.txt
              cat "$log_file" >> error-log.txt
            else
              echo "✅ Aucun log trouvé pour $service." >> error-log.txt
            fi
          done
          git add error-log.txt
          git commit -m "Ajout des logs d'erreur via GitHub Actions" || echo "Aucune modification à commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

      - name: 🔀 Créer une Pull Request vers `develop` si aucun log d'erreur n'est trouvé
        if: success()
        run: |
          gh pr create --base develop --head ${{ github.ref_name }} --title "Merge successful deployment" --body "Tous les services se sont déployés correctement."
